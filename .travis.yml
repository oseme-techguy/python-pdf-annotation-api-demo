sudo: required
services:
  - docker
language: python
install:
  - pip install pipenv
  - pipenv install
# command to run tests
# script:
# unit test
# – python -m pytest -v
# linting test
# – flake8
after_success:
  - sh .travis/deploy_dockerhub.sh
